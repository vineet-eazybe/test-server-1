{
  "name": "eazybe-webhook-receiver",
  "version": "1.0.0",
  "description": "Google Cloud Function for handling WhatsApp webhooks and processing broadcasts",
  "main": "index.js",
  "scripts": {
    "start": "functions-framework --target=webhookReceiver",
    "deploy": "gcloud functions deploy webhookReceiver --runtime nodejs20 --trigger-http --allow-unauthenticated"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^3.0.0",
    "@google-cloud/pubsub": "^4.0.0",
    "axios": "^1.7.0",
    "medici": "^5.0.0",
    "mongoose": "^7.6.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
